<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs', { title: 'Productos', stylesheet: 'products/productsList.css'}); %>
    <body >
        <%- include('../partials/header.ejs') %>
        <div class="flexListado">
            <h1 class = "titulo">Tus Productos</h1><br><br>
            <% for ( product of products) { %>  
            <div>
                <img class="imagenListado" src="/images/products/<%= product.image %> "alt="<%= product.name %>"></a>                
                <div class="productosListado">
                    <p> <%= product.name %> </p>
                    <p> $<%= product.price %> </p>
                    <p> <%= product.category %> </p>
                    <p class="descripcionProducto"><%= product.description %></p>                 
                </div>
            </div>
            <% } %>
        </div>       
        <%- include('../partials/footer.ejs') %>    

        
    </body>
        
</html>


