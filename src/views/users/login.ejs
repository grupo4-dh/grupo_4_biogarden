<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/head.ejs', { title: 'Iniciar sesión', stylesheet: 'users/login.css'}); %>
    <body>
        <%- include('../partials/header.ejs') %>
        <div class = "container-error"><% if (typeof errors != "undefined" ){%>
            <p class = "error" style="color: red;">ERROR</p>
            <ul>
                <% for (let i=0; i<errors.length;i++){ %>
                    <li>
                        <%= errors[i].msg%>
                    </li>

                    <%}%>
            </ul>

        <%}%>
    </div>
        <form class = "flex" action ='/users/login' method="POST">
            <input type="email" id="Email" name="email" class = "emailInput" placeholder = "Email..."><br>
            <input type="password" id="pwd" name="password"class = "emailInput" placeholder = "Contraseña..."><br>
            <ul class= "flex2">
            <a href="">¿Olvidaste tu contraseña?</a>
            <a>|</a> <a href="/registro">Registrarse</a>
            <br><br>
            </ul>
            <input type="submit" value="Iniciar Sesion" class = "botonInicio">
            
        </form>
        <%- include('../partials/footer.ejs') %>
    </body>
</html>